# Group A: v1.2.0 + v1.3.0 — Workflow Intelligence + Orchestration

## v1.2.0 — Workflow Intelligence and Authoring Quality
Files: workflows/player.py, workflows/recorder.py, workflows/schema.py, workflows/selector_generator.py, tests/

### P1: Selector auto-heal and confidence scoring
- In selector_generator.py: add confidence scoring for selectors (ID/stable > class > tag)
- In player.py: when a step fails with element-not-found, try auto-healed selectors (strip dynamic class names, try parent-child fallbacks)
- In recorder.py: add confidence score to recorded steps, annotate low-confidence steps

### P2: Rehearsal mode + pre-save validation
- In schema.py: add `validate_workflow_steps()` that checks for common mistakes (missing verify after fill, no timeout on navigate, etc.)
- Add `rehearse(workflow)` method to player.py — executes in dry-run mode (takes screenshots, logs selectors, never clicks/submits)
- Add tests for rehearsal mode

### P3: Workflow versioning/diff
- Add metadata version field to workflow schema (v1.0.0)
- In recorder.py: save metadata changelog notes on each save
- Add `workflow_diff(old, new)` utility function

## v1.3.0 — Orchestration and Automation Operations
New module: production/workflow_orchestrator.py

### P1: Queue manager with per-domain/account concurrency
- Create WorkflowOrchestrator class with queue management
- Per-domain concurrency limits (e.g. max 1 concurrent LinkedIn workflow)
- Queue persistence to disk (JSON-based)
- Transient failure auto-retry with exponential backoff

### P2: Scheduled + recurring execution
- Add cron-like scheduling to orchestrator (datetime-based, not system cron)
- Recurring workflow support with interval config
- Checkpoint persistence for resume capability

### P3: Cross-workflow composition
- In player.py: support workflow chaining (run_workflow step with variable passing)
- Cycle detection in dependency chains
- Error propagation between chained workflows

### Tests
- Unit tests for all new modules
- Tests for queue persistence, scheduling, cycle detection
- Rehearsal mode tests
- Selector auto-heal tests

## Dont
- Don't commit, push, or create PRs
- Don't modify existing workflow YAML files
- Don't modify mcp_security.py, anti_block_orchestrator.py

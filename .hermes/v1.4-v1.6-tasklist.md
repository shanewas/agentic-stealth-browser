# Group B: v1.4.0 + v1.5.0 + v1.6.0 — Security, Scale, SDK

## v1.4.0 — Security and Governance
Files: mcp_security.py, production/, tests/

### P1: MCP trust model hardening
- Add rate limiting for tool calls per session (already partially done in ToolRateLimiter, wire it up in mcp_server.py)
- Add input validation for all MCP tool parameters (max length, type checks, allowed patterns)
- Add session isolation enforcement (one session's tools can't access another session's data)

### P2: Workflow policy engine
- Create production/policy_engine.py with path/action/destination controls
- Workflow execution policy: which sites, which step types are allowed
- Policy file format (YAML) in ~/.agentic-browser/policies/

### P3: Approval gates and audit enrichment
- Add approval gate hooks for sensitive actions (navigate to unknown domain, execute_js, run_workflow)
- Enhance audit logger with actor/session/workflow correlation
- Add audit export endpoint to MCP server

## v1.5.0 — Scale and Performance
Files: core/agent_browser.py, production/, tests/

### P1: Profile bottlenecks
- Add timing instrumentation to safe_goto, safe_click, safe_type (already partially done in MetricsCollector)
- Create performance benchmarking script (scripts/perf_benchmark.py)
- Document performance tuning guide (docs/PERFORMANCE_TUNING.md)

### P2: Context/session reuse
- Improve session checkpoint save/load performance
- Add lazy initialization for heavy components (stealth scripts, TLS managers)
- Reduce redundant init_script injection on context reuse

### P3: Stress harness
- Create endurance test script (scripts/endurance_test.py) — 8h run simulation
- Memory leak detection via objgraph or tracemalloc snapshots
- Resource ceiling checks (CPU%, memory, open FDs)

## v1.6.0 — API/SDK and Plugin Ecosystem
Files: new module production/sdk/, tests/

### P1: Python SDK
- Create production/sdk/client.py with workflow lifecycle API (create, execute, get_status, list)
- Proper async client with timeout and error handling
- Type-hinted public interfaces

### P2: MCP contract normalization
- Standardize all MCP tool response shapes (status, error_code, message, data)
- Add JSON Schema output for every tool
- Typed dicts for all tool parameters

### P3: Plugin template
- Create plugin template directory (plugins/template/)
- Example plugin: screenshot-notifier
- Plugin loading mechanism (entry_points or config-based)

## Dont
- Don't commit, push, or create PRs
- Don't modify workflow YAML files

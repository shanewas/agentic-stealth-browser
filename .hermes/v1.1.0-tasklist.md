# v1.1.0 — Reliability, CI, and Backlog Convergence

## Context
Repo at `/root/agentic-stealth-browser`, current tag v1.0.1.
All changes are in this repo. DO NOT commit, push, or create PRs.

## Tasks

### P2: CI/Test Hardening
1. **Expand test coverage** (issue #6):
   - Add unit tests for Recovery state machine (backoff calculation, BlockType detection, circuit breaker)
   - Add unit tests for RateLimiter (window tracking, cap enforcement, per-account isolation)
   - Add unit tests for MetricsCollector (timers, counters, redaction)
   - Add unit tests for CookieManager (validation, expiry, domain filtering)
   - Add unit tests for Workflow schema validation (missing fields, unknown types)
   - Add unit tests for ProxyManager (config validation, site sensitivity, health tracking)

2. **Create/update CI pipeline** (issue #21):
   - Create `.github/workflows/ci.yml` with:
     - ruff/black linting
     - mypy gradual type checking
     - pytest unit test suite (non-browser tests only)
     - Docker build smoke test
   - Ensure CI caches Playwright browsers between runs
   - Fail CI on lint errors or test failures (not just warnings)

### P3: Production Runtime Hardening
3. **Fix detection_runner.py naming bug** (issue #23):
   - Fix `browser.browser.content()` → `browser.page.content()` in `tests/detection_runner.py`
   - Enable nightly detection regression scheduling

4. **Harden Dockerfile** (issues #13, #15):
   - Create non-root user (`appuser`)
   - Add HEALTHCHECK (curl-based or python health endpoint)
   - Add volume mounts for `~/.agentic-browser/sessions`, screenshots, logs
   - Proper COPY with .dockerignore (exclude .git, __pycache__, tests, .clawpatch, REVIEW_*.md)
   - Add proper CMD/ENTRYPOINT for the MCP server
   - Create `.dockerignore`

5. **Make rate limiting default** (issue #20):
   - Add `rate_limit: bool = True` param to `safe_goto`
   - Inside safe_goto, always call the rate limiter when rate_limit=True
   - Document opt-out path

### Additional v1.1.0 improvements
6. **Add Docker healthcheck endpoint** in production/mcp_server.py
7. **Add operator documentation** for production deployment
8. **Create upgrade notes** for v1.0 → v1.1 migration

## Files to touch
- tests/*.py (new test files)
- .github/workflows/ci.yml (create)
- production/Dockerfile (harden)
- .dockerignore (create)
- core/agent_browser.py (rate_limit param in safe_goto)
- tests/detection_runner.py (fix bug)
- production/mcp_server.py (health endpoint)
- docs/ (deployment guides)

## Dont
- Don't commit, push, or create PRs
- Don't change any existing test that passes
- Don't modify workflow YAMLs or recorder code

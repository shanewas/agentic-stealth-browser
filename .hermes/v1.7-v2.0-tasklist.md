# Group C: v1.7.0 + v1.8.0 + v1.9.0 + v2.0.0 — Expansion, Learning, Migration, GA

## v1.7.0 — Browser/Platform Expansion
Files: core/agent_browser.py, stealth/, tests/

### P1: Chromium/Edge parity
- Document known differences between Chromium and Edge CDP
- Add platform capability detection
- Edge-specific launch args and stealth adjustments

### P2: Firefox adapter (feature-flagged)
- Create stealth/firefox_adapter.py with Firefox-specific stealth patches
- Feature flag: FIREFOX_SUPPORT=true
- Basic navigation and click support

### P3: Capability map
- Create docs/CAPABILITY_MAP.md listing all features and their browser/backend support
- Add get_client_capabilities() method that returns supported features dynamically
- Unsupported features return actionable error messages

## v1.8.0 — Adaptive Stealth and Learning Loop
Files: behavior/adaptive_tuner.py, recovery/, tests/

### P1: Telemetry feedback ingestion
- Collect replay/recovery events into a feedback store
- Track selector success rates per domain
- Store stealth detection events (when blocks happen, what bypassed them)

### P2: Adaptive behavior tuning
- BehaviorTuner learns from telemetry: adjusts typing speed, scroll patterns, mouse variance
- Domain-specific tuning profiles
- Bounded adaptation (never reduces below minimum stealth thresholds)

### P3: Evaluation harness
- Create scripts/evaluate_stealth.py comparing patched vs baseline behavior
- Generate comparative reports (success rate, detection rate, timing)
- Reproducibility gate: same seed = same results

## v1.9.0 — v2 Migration Line
Files: production/, docs/, tests/

### P1: v2 RFC document
- Create docs/rfc/v2-migration.md with all planned breaking changes
- List deprecated APIs and their replacements
- Timeline for v1 deprecation

### P2: Deprecation warnings
- Add DeprecationWarning to all APIs that will change in v2
- Create compatibility shims in production/deprecations.py
- Log structured deprecation warnings at warn level

### P3: Migration validators
- Create scripts/migrate_v1_to_v2.py — converts v1 workflow YAMLs to v2 format
- Add CI migration check step
- Validate that v1 workflows load correctly under v2 shims

## v2.0.0 — Workflow Platform GA
Files: all

### P1: Finalize v2 contracts
- Workflow schema v2 with stricter typing
- Unified execution result envelope across CLI/MCP/SDK
- Remove all deprecated aliases/shims

### P2: Release candidate
- Cut 2.0.0-rc tag
- Run full CI matrix
- Validate against all workflow library files

### P3: GA
- Update SERVER_VERSION to 2.0.0
- Publish migration guide
- Tag v2.0.0

## Dont
- Don't commit, push, or create PRs

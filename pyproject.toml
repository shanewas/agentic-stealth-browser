[project]
name = "agentic-stealth-browser"
version = "0.8.0-phase8"
description = "Stealth browser automation"
requires-python = ">=3.10"
dependencies = ["playwright", "aiohttp"]
[project.scripts]
stealth-browser = "production.cli:main"
[tool.setuptools.packages.find]
where = ["."]
